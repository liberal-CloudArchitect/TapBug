# 作用域：本文件仅用于隔离的教学扩展 extensions/hermes_ctf_lab（exploit_agent LLM 推理后端）。
# 它与受治理主包 src/hermes 无关——主包调用的模型由 run 配置的 "model" 字段 / --model / HERMES_E2E_MODEL
# 指定（当前为 deepseek-v4-flash），不读取以下 HERMES_LLM_* 变量。
#
# 复制为 .env 并填入你的 key，即可启用 exploit_agent 的 LLM 推理后端。
# 仅用于授权 CTF/靶场的按题推理利用；不填则自动回退到确定性 heuristic 后端。
# 支持多厂商：Anthropic(SDK) + 任意 OpenAI 兼容端点（DeepSeek / OpenAI / Moonshot 等，httpx）。

# --- 方式一：DeepSeek（OpenAI 兼容，已 live 验证）---
HERMES_LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-...
HERMES_LLM_MODEL=deepseek-v4-flash

# --- 方式二：OpenAI ---
# HERMES_LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# HERMES_LLM_MODEL=gpt-4o-mini

# --- 方式三：Anthropic ---
# HERMES_LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# HERMES_LLM_MODEL=claude-haiku-4-5-20251001

# --- 方式四：任意 OpenAI 兼容端点 ---
# HERMES_LLM_BASE_URL=https://your-endpoint/v1
# HERMES_LLM_API_KEY=...
# HERMES_LLM_MODEL=your-model
