# Hermes 需求追踪矩阵

> 基线：PRD 1.6（2026-07-29）  
> 状态语义：**已实现（单元/契约）** 不等于产品 E2E 已验收；**部分实现** 表示关键组件或证据链缺失；**未验收** 表示外部运行时、真实 Docker/网络或完整 CLI 链路未通过。

## 1. 功能需求

| ID | 来源版本 | 实现组件 | 主要验收证据 | 当前状态 |
|---|---|---|---|---|
| GOV-01 | v2；v1 FR1 的安全收紧 | `runtime/policy.py`, `runtime/context.py` | `tests/test_policy_runtime.py`, `tests/test_orchestrator.py`，Phase 2 local E2E | **最小链已验收**：系统 DNS、scope 和 pinned loopback transport 实际运行；真实域名 profile 未验收 |
| GOV-02 | v2 | `runtime/gateway.py`, `runtime/transport.py` | `tests/test_policy_runtime.py`, `tests/test_pinned_transport.py`，Phase 2 local E2E | **部分实现**：HTTP Gateway/pinned transport 已在 localhost E2E 运行；命令输出/凭据 broker 尚未闭环 |
| GOV-03 | v2；v1 FR6 具体化 | `vertical_contracts.py`, `security.py`, `cli.py`, `vertical.py` | `tests/test_phase2_security.py`, CLI 状态测试，accepted/reject E2E | **最小链已验收**：Approver/Reviewer 分钥、ApprovalBundle、拒绝和两次跨进程暂停恢复均通过 |
| GOV-04 | v2 | `runtime/context.py`, `workflow.py`, `vertical.py`, CLI `retry` | `tests/test_run_resume.py`, `tests/test_workflow_reliability.py`, `tests/test_phase2_recovery.py`, `test_legacy_v1_read_only.py` | **已实现（保守恢复契约）**：暂停点可恢复；失败 run 保留并以 fresh run + 新审批重试；V1 public start/resume 在任何角色启动前 `legacy_run_read_only`；真实多进程崩溃恢复未验收 |
| GOV-05 | v2；阶段 3 收紧 | `domain_contracts.py`, `promotion.py`, `preflight.py`, `reporting.py` | `test_domain_contracts_v2.py`, `test_report_preflight_v2.py`, `test_reporting_boundary.py`，当前 V2 accepted + strict tamper | **固定 V2 链已验收**：PromotionService 独占晋升，Reporter 前后统一 preflight，父运行时重算 Link/XCTO/状态/body；7/7 strict tamper fail-closed，正式报告数为 0 |
| GOV-06 | v2；阶段 3 收紧 | `evidence.py`, `runtime/gateway.py`, ADR 0005 | `test_evidence_store_v2.py`, `test_gateway_evidence_v2.py`，当前 V2 accepted E2E | **固定 V2 链已验收**：3 份 manifest + analysis，Recon 无审批绑定，两份 Verifier evidence 精确绑定 action/plan/bundle/consumption；analysis 为 579/592/592 bytes；AES-GCM raw 仍以单元/契约验收 |
| AGT-01 | v1 | `providers/acp.py`, `scripts/restricted_hermes_acp.py`, `vertical_v3.py` | `tests/test_acp_provider.py`, `tests/test_vertical_v3.py`，Phase 4 accepted E2E | **V3 最小协作链已验收**：16 个独立 ACP session；协议只接受结构 JSON，tool/permission fail-closed，V3 verifier 的运行边界字段由父运行时绑定 |
| AGT-02 | v1+v2 | `runtime/agents/contracts.py`, `domain_contracts_v3.py`, `prompts/v3/registry.json` | `test_agent_contracts_v2.py`, `test_prompt_registry.py`, `test_domain_contracts_v3.py`，Phase 4 E2E | **V3 九角色固定契约已验收**：prompt/manifest/contract digest 强绑定，四个分支仅能 assessment 或 cross-review；通用角色插件化仍未验收 |
| AGT-03 | v1 | `cli.py`, `vertical.py`, `vertical_v3.py`, `RunnerHost`, role-runtime | `tests/test_vertical_v3.py`, `test_cli_v3.py`，Phase 4 accepted E2E | **固定 V3 协作链已验收**：16 个容器、角色进程与 ACP session 独立，完成 fan-out/fan-in、两次人工门和报告 |
| AGT-04 | v1 FR4 | `vertical_v3.py`, `collaboration_v3.py`（`RoutePolicy` 在此，非独立 `route_v3.py`） | `test_vertical_v3.py`, `test_collaboration_v3.py`，Phase 4 accepted + P4-Resilience E2E | **固定 localhost 并行协作已验收**：四路最多并发 4、确定性 fan-in、去重、环形交叉复核；API 单分支真实失败后仍以精确 gap 进入 `completed_with_gaps`。多资产路由未验收 |
| AGT-05 | v1 NFR5 | `ledgers_v3.py`, `vertical_v3.py`, `runtime/gateway.py` | `test_ledgers_v3.py`, `test_vertical_v3.py`，Phase 4 accepted E2E | **固定 V3 链已验收**：ActionLedger 防止重复探索，BudgetLedger 预留 16 次实际 prompt attempt/4,000,000 microusd，15 次请求在预算内；真实费用质量基线仍未验收 |
| CAP-01 | v3 | `knowledge.py` `ProblemCard`, `KnowledgeBroker` | `tests/test_knowledge_governance.py` | **部分实现**：内容寻址和去重已测；尚未由评估工作流触发 |
| CAP-02 | v3 | `r25_workflow.py` Researcher、`ResearchGateway`、legacy `KnowledgeBroker` | `tests/test_r25_workflow.py`, `tests/test_r25_e2e_driver.py`, `tests/test_knowledge_governance.py` | **受治理缩减链已验收**：Researcher 真实 ACP 只消费 local archive 的受限 projection；公网 ResearchGateway 仍未产品级验收 |
| CAP-03 | v3 | `r25_workflow.py` Planner、`r25_parser.py`、legacy `WheelGenerator` | `tests/test_r25_parser.py`, `tests/test_r25_workflow.py`, R2.5 E2E | **固定模板已验收**：父运行时约束 field schema，生成器只支持 `line_kv_parser/v1`；通用能力生成未验收 |
| CAP-04 | v3 | `r25_parser.py`、`wheels/validator.py` | `tests/test_r25_parser.py`, `tests/test_r25_workflow.py`, R2.5 E2E | **固定 parser 已验收**：静态检查、正反例和无网 Docker 验证通过；更广语义质量门仍小 |
| CAP-05 | v3 | `wheels/sandbox.py`、R2.5 `DockerSandbox`/`CapabilityHostV2` | `tests/test_docker_sandbox_integration.py`, `tests/test_r25_e2e_driver.py` | **R2.5 被动 parser 已真实验收**：digest-pinned 无网、只读、non-root sandbox；一般 Wheel/真实资产 profile 未验收 |
| CAP-06 | v3 | `wheels_v2/registry.py`、`r25_workflow.py`、legacy registry | `tests/test_wheels_v2.py`, `tests/test_r25_workflow.py`, R2.5 E2E | **R2.5 lifecycle 已真实验收**：五方独立密钥、8 条签名事件、active selector 与 quarantine 路径；通用 selector 未验收 |
| CAP-07 | v3 | `learning_recovery.py`（契约+守卫）、`workflow.py` 的知识角色条目、registry usage | `tests/test_learning_recovery.py`（12 例）；无真实 pause→Wheel→恢复 E2E | **契约层已实现（单测）**：`learning_recovery.py` 提供 pause 记录、resume 绑定与效果回写契约，守卫强制「仅 active 已批 Wheel 解锁、禁止原地恢复、绑定冻结输入」；**仍未实现**：`workflow.py` DAG 接线与真实 V3/V4 pause→resume E2E（待基础设施，见 `docs/15` §10.3） |
| DET-01 | v1 | `vertical.py`, `vertical_v3.py`, role-runtime Recon/Mapper, local lab | `tests/test_phase2_runtime.py`, `test_vertical_v3.py`，Phase 4 E2E | **固定本地链已验收**：Recon 1 GET、Link/GraphQL/debug/identity 元数据映射和真实 socket evidence 通过 |
| DET-02 | v1 | `collaboration_v3.py`, V3 Web/API/Authz/Infra prompts | `test_collaboration_v3.py`, `test_vertical_v3.py`，Phase 4 E2E | **固定四候选已验收**：5 个原始候选确定性去重为 XCTO、GraphQL、Authz、debug 四个规范候选；通用漏洞族未验收 |
| DET-03 | v1+v2 | `campaign_v3.py`, `ledgers_v3.py`, `vertical_v3.py` | `test_campaign_v3.py`, `test_ledgers_v3.py`，Phase 4 E2E | **固定 V3 最小验证已验收**：只读和状态变更两批审批、14 个原子 consumption、15 次总请求与补偿恢复均通过；真实目标/协议未验收 |
| DET-04 | v1 FR7+v2+阶段 3 | `preflight_v3.py`, `reporting_v3.py`, `promotion_v3.py` | `test_preflight_v3.py`, `test_promotion_reporting_v3.py`, Phase 4 + P4 tamper E2E | **固定 V3 报告硬门已验收**：4 finding、coverage、签名 review、launch/ack/write receipt 完整；12 类真实复制篡改均使 fresh preflight 在 Reporter launch 前拒绝，正式报告不存在 |
| DET-05 | v1 Phase 3+v2 质量门 | `quality.py`, `quality_v4.py`, `promotion_v4.py`, `tests/fixtures/quality`（含 V4 `ground-truth-v2.json` 320 例） | `tests/test_assessment_quality.py`，V4 quality receipt | **部分实现**：V4 已引入 320-case 冻结数据集并由 preflight 独立重算 recall/precision（仅固定教学数据集）；**仍未验收**：无真实扫描 ground truth、独立 benchmark（XBOW/Cybench 隔离）、成本或时延结果 |

## 2. 非功能需求

| ID | 来源版本 | 实现组件 | 主要验收证据 | 当前状态 |
|---|---|---|---|---|
| NFR-01 | v2 | Policy/Gateway、包隔离、Wheel 权限 | `test_policy_runtime.py`, `test_packaging_isolation.py`, `test_docker_sandbox_integration.py`，当前 V2/R2.5 local E2E | **固定 localhost 部署链与 R2.5 sandbox 已验收**；真实资产 profile 仍未验收 |
| NFR-02 | v2 | `ledgers_v3.py`, `preflight_v3.py`, Audit/Event/Registry 链 | `test_preflight_v3.py`, `test_phase4_acceptance_matrix.py`，Phase 4 + P4 tamper E2E | **固定 V3 链已验收**：route、dedup、review、approval、consumption、evidence、action/budget ledger、cleanup、coverage 与报告收据均经 hash 回溯；12 类真实篡改被 fail-closed。通用身份治理未验收 |
| NFR-03 | v1+v3 | RoleManifest、ROLE_POLICIES、WheelRegistry | `test_runner_host.py`, `test_registry_governance.py` | **部分实现**；新 agent 仍需改核心策略表，未达插件化验收 |
| NFR-04 | v1+v2 | `cli.py`, `vertical_v3.py`, ledgers、coverage | `test_cli_v3.py`, `test_vertical_v3.py` | **固定 V3 链已验收**：`status` 重算分支、预算、ledger、网络、cleanup 和下一动作；仍无完整 UI/长期指标视图 |
| NFR-05 | v2 | RunContext、事件链、超时 | `test_run_resume.py`, `test_workflow_reliability.py`, `test_runner_host.py`，当前真实 E2E | **最小链已验收**：两个暂停点跨 CLI 进程恢复通过；任意阶段崩溃恢复仍需扩展 |
| NFR-06 | v2+v3 | `requirements.lock`、digest manifest/image、hash artifacts | 构建测试和 registry 测试 | **部分实现**；`requirements.lock` 仅固定顶层依赖且无 hash，无完整可复现镜像流程 |
| NFR-07 | v1 NFR5 | `ledgers_v3.py`, `vertical_v3.py` | `test_ledgers_v3.py`, `test_vertical_v3.py`，Phase 4 E2E | **固定 V3 链已验收**：40 attempt/10 美元/30 分钟/180 秒任务/并发 4 护栏，真实 run 16 attempts、4,000,000 microusd；token 与真实费用质量基线未验收 |
| NFR-08 | v1 NFR6+v2 adapter 重构 | Python 包、Skill、AgentRunner 接口 | `test_packaging_isolation.py`，当前 Hermes ACP V2 E2E | **部分实现**；Hermes ACP 适配器已在固定链验收，但 Skill 安装面及 Claude/Codex/其他 provider 未验收 |

## 2.1 逐项证据路径与外部条件

上面两张表给出每项需求的来源、组件、测试和状态；本表以相同 ID 补齐**权威 evidence path**与
**外部条件**。两表按 ID 连结后共同构成 FR/NFR → 组件 → 测试 → 状态 → 工件路径 → 条件的唯一
追踪矩阵。`E-V2`、`E-V3`、`E-P4` 和 `E-R25` 均为仓库相对路径，允许审计者直接重放或检查。

| ID | 权威证据路径 | 外部条件 / 未外推边界 |
|---|---|---|
| GOV-01 | E-V2；`tests/test_policy_runtime.py` | localhost scope/DNS 已验收；真实域名 RoE/profile 未验收 |
| GOV-02 | E-V2；`tests/test_pinned_transport.py` | loopback pinned transport；真实凭据/命令 broker 未验收 |
| GOV-03 | E-V2；`docs/10-阶段2密钥轮换与本地验收.md` | 本地 approver/reviewer 私钥；真实项目审批流程未验收 |
| GOV-04 | E-V2；E-P4 crash root | V2 暂停恢复、V3 mutation crash 已验收；任意外部宕机组合未验收 |
| GOV-05 | E-V2；`docs/11-阶段3领域契约与证据链验收.md` | 固定 candidate/report；真实资产 finding 不可据此声称 |
| GOV-06 | E-V2；`tests/test_evidence_store_v2.py` | raw AES-GCM 契约/本地工件；真实敏感数据保留策略未验收 |
| AGT-01 | E-V3；`tests/test_acp_provider.py` | Hermes Agent 0.16/ACP 1/local model；其他 provider 未验收 |
| AGT-02 | E-V3；`tests/test_prompt_registry.py` | 固定九角色；通用插件角色未验收 |
| AGT-03 | E-V3；`tests/test_vertical_v3.py` | localhost teaching fixture；真实资产流程未验收 |
| AGT-04 | E-V3、E-P4；`tests/test_collaboration_v3.py` | 最多四分支、本地固定路由；多资产路由未验收 |
| AGT-05 | E-V3、E-P4；`tests/test_ledgers_v3.py` | 40 attempts/$10 固定预算；真实费用质量数据未验收 |
| CAP-01 | `tests/test_knowledge_governance.py` | legacy ProblemCard 未接到 V3；R2.5 用操作员观察替代 |
| CAP-02 | E-R25；`tests/test_r25_e2e_driver.py` | 本地归档资料；公网 allowlist Gateway 未验收 |
| CAP-03 | E-R25；`tests/test_r25_parser.py` | 仅 `line_kv_parser/v1`；任意生成器未验收 |
| CAP-04 | E-R25；`tests/test_r25_workflow.py` | 固定正/反例；通用语义测试未验收 |
| CAP-05 | E-R25；`tests/test_docker_sandbox_integration.py` | immutable local sandbox；真实资产 Wheel 未验收 |
| CAP-06 | E-R25；`tests/test_wheels_v2.py` | 五方临时签名密钥；企业 KMS/通用 registry 未验收 |
| CAP-07 | `tests/test_learning_recovery.py`；PRD 6.3 | 契约+守卫层已实现并单测（禁止原地恢复、仅 active Wheel 解锁）；workflow 接线与真实 E2E 待基础设施 |
| DET-01 | E-V2、E-V3；`tests/test_phase2_runtime.py` | 固定 localhost endpoint/metadata；真实攻击面未验收 |
| DET-02 | E-V3；`tests/test_collaboration_v3.py` | 四种 fixture candidate；任意漏洞族未验收 |
| DET-03 | E-V2、E-V3；`tests/test_campaign_v3.py` | 签名本地 approval；真实资产主动 PoC 未授权 |
| DET-04 | E-V2、E-V3、E-P4；`tests/test_promotion_reporting_v3.py` | 内部报告草稿；平台提交由人工负责 |
| DET-05 | `tests/test_assessment_quality.py` | 仅小 fixture；独立 ground truth/R3 未验收 |
| NFR-01 | E-V2、E-R25；`tests/test_docker_sandbox_integration.py` | localhost Docker；真实资产部署未验收 |
| NFR-02 | E-V3、E-P4；`tests/test_preflight_v3.py` | 固定 artifact schema；通用身份治理未验收 |
| NFR-03 | `tests/test_runner_host.py`、E-R25 | manifest 固定角色/模板；插件生态未验收 |
| NFR-04 | E-V3；`tests/test_cli_v3.py` | CLI JSON 状态；无长期运维 UI |
| NFR-05 | E-V2、E-P4 crash root；`tests/test_run_resume.py` | 既定暂停/crash 场景；所有故障模型未验收 |
| NFR-06 | E-V2、E-V3、E-R25；registry tests | digest/manifest 固定；依赖全 hash/clean CI 未验收 |
| NFR-07 | E-V3；`tests/test_ledgers_v3.py` | local model attempts/cost reserve；实际 token billing 未验收 |
| NFR-08 | E-V2、E-V3；`tests/test_packaging_isolation.py` | Hermes ACP 已验收；其他 provider/Skill 安装未验收 |

证据根定义：E-V2 = `artifacts/phase3-authoritative-e2e/20260714T044246Z-phase2-fe73e3b1b378/`；
E-V3 = `artifacts/phase4-e2e/20260728T132742Z-phase4-298f439ad18a/`；E-P4 =
`artifacts/phase4-resilience-e2e/{20260729T052018Z-phase4-c71ce45c2db2,20260729T054101Z-phase4-55b13e23ad04,20260729T060212Z-phase4-78227fb9ca70,20260729T061322Z-phase4-e193c1e9e532}/`；
E-R25 = `artifacts/r25-e2e/20260729T053806Z-r25-15e07a341d55/replay-summary.json`。

## 3. 阶段验收总览

| 阶段 | 覆盖需求 | 当前证据结论 |
|---|---|---|
| R0 | GOV-04, NFR-05/06/08，主包/CTF 隔离 | 核心单元和构建证据存在；干净环境安装和支持环境尚应固化 |
| R1 | GOV-01‑06, NFR-01/02 | 核心单元/契约完成，pinned loopback transport 已在宿主集成测试和 R2 E2E 通过；真实域名、凭据使用和命令证据仍未验收 |
| R2 | AGT-01‑05, DET-01‑04 | **固定最小纵向闭环已验收**：当前真实 ACP + 六 Docker 容器 + pinned loopback + accepted/reject E2E 通过；并行和更多漏洞类型未完成 |
| 阶段 3 / R2 hardening | GOV-05/06, AGT-01/02, DET-03/04 | **固定 localhost V2 链已验收**：当前 accepted/reject、7 类 strict tamper、3 evidence + analysis、2 consumptions 和正式报告硬门通过；历史 V1 run 仍只读 |
| 阶段 4 / V3 协作 | AGT-01‑05, DET-01‑04, NFR-02/04/07 | **固定 localhost Phase 4 + P4-Resilience 已验收**：基线为 16 独立任务/容器/ACP session、四路并行、去重、交叉复核、两批审批、补偿、15 evidence 请求、14 consumptions、4 finding 和报告收据；另有 API 隔离、cleanup-only 恢复、crash 不重放与 12 类篡改真实门。多资产、真实 profile 与通用候选未验收 |
| R2.5 | v3 CAP 的受治理缩减实现 | **首个 local-lab passive-parser 闭环已真实验收**：独立 R2.5 contract、五方 trust、signed registry、typed ACP 两角色、固定 parser、child-run continuation 和 CLI 全链通过；`20260729T053806Z-r25-15e07a341d55/replay-summary.json` 证明 2 session/container/PID、8 registry events、正反例和 0 Wheel 请求。Wheel 不恢复/改写 V3、不会自动 promotion/finding/report；P4-Resilience 也已通过，但真实资产仍需独立 profile/RoE 验收。 |
| R3 | DET-05 及跨阶段质量门 | 只有最小质量基线；不得解读为真实检测或赏金发现效果 |

## 4. 当前验证基线

- `pytest`：409 passed，3 skipped（2026-08-01 实跑）；skip 仅为未配置外部 sandbox image 与历史 Phase 2 real-E2E artifact 的可选复验。历史基线（299@2026-07-28 等）见各阶段验收文档，仅作历史记录。
- Ruff lint、Ruff format、strict mypy、prompt/manifest registry validate：通过。
- Python wheel：可构建，主包不包含 CTF/legacy 代码。
- `prompt_attempts` 是 provider metadata 必填正整数；缺失、0、超界和 `True`/`False` 均被 preflight 拒绝，Coverage 的 `model_calls` 由其求和。
- 权威根：`artifacts/phase3-authoritative-e2e/20260714T044246Z-phase2-fe73e3b1b378/`。
- accepted `232c6b5c-9e6d-4933-830c-d10be1f7a5b6`：completed，六角色/容器/ACP session、3 evidence + analysis、2 consumptions、candidate 2/control 1。
- rejected `f3650961-a210-4c2a-bfb0-d88c613f32c8`：1 Recon evidence、`requests_used=1`、rejected。
- strict tamper：7/7 blocked、`formal_reports_created=0`，路径 `tamper-replay-strict/summary.json`。
- Coverage：`model_calls=6`（Recon schema repair=2，其余 Reporter 前角色各 1）、
  `elapsed_ms=221031`、`cost_microusd=null`（provider 未给出）、steps 2/2、finding 1。
- analysis 大小为 579/592/592 bytes；pinned transport 通过；验收后 `docker ps` 无遗留容器。
- 历史 V1 accepted/reject 仍是只读审计基线。
- Phase 4 权威根：`artifacts/phase4-e2e/20260728T132742Z-phase4-298f439ad18a/`；全批准 accepted `81764e3a-61df-4890-87bf-80691a8fc99f`：16 task/container/process/session、15 evidence/request、14 consumption、4 finding、cleanup restored、无遗留容器。真实变体还覆盖 Web+Infra 动态子集（10/5/2）、mutation 拒绝（14/5/2）、readonly 拒绝（14/11/2 + cleanup restored）和双批拒绝（11/1/0，正式报告 0）；P4-Resilience 又通过 API 隔离、cleanup-only 恢复、crash 不重放及 12 类 tamper，详见 `docs/12`。
- R2.5 权威根：`artifacts/r25-e2e/20260729T053806Z-r25-15e07a341d55/replay-summary.json`；两个真实 ACP role/session/container/PID、8 条 Wheel registry 事件、正例匹配、反例 no-match、Wheel 0 请求、0 正式报告。它仅是独立 child continuation 的被动观察，不等价于“暂停 V3 → Wheel → 原地恢复评估”；后者被当前冻结边界明确禁止。仍未验收：多资产调度、真实资产/身份 profile、任意漏洞族和 R3 独立质量基准。
